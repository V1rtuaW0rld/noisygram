# noisygram
Noise Counter: A client application that listens for sounds and sends them to a server, which determines whether the sound matches the target noise and logs it into a graph. A dashboard tracks and aggregates all detections, and a player lets you replay every captured audio event.
